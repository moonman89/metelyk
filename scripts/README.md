# Scripts

| Script | Purpose |
|--------|---------|
| `convert-catalog-usd.mjs` | Convert UAH catalog to USD + oz weights |
| `sync-catalog-to-functions.mjs` | Copy `data/catalog.json` into `functions/` before deploy |
| `rename-assets.mjs` | One-time: hash filenames → `{slug}` paths (already run) |
| `dev/test-openai-tea-guide.mjs` | Smoke-test ChatGPT function |
| `dev/test-gemini.mjs` | Smoke-test Gemini (optional) |

Docs: see `/docs/firebase-setup.md` and `/docs/tea-assistant-setup.md`.
